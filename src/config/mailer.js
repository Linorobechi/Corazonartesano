import {
  transporter,
  createTransporter,
  getMailConfig,
  sendResetPasswordEmail,
} from "../../server/config/mailer.js";

export {
  transporter,
  createTransporter,
  getMailConfig,
  sendResetPasswordEmail,
};

export default {
  transporter,
  createTransporter,
  getMailConfig,
  sendResetPasswordEmail,
};