{
  "name": "corazon-artesano-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n \"FRONTEND,BACKEND\" -c \"cyan,magenta\" \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:full": "concurrently -n \"FRONTEND,BACKEND\" -c \"cyan,magenta\" \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@rolldown/binding-win32-x64-msvc": "^1.2.8",
    "@supabase/supabase-js": "^2.116.0",
    "asynckit": "^0.5.0",
    "lightningcss-win32-x64-msvc": "^1.33.0",
    "pg-types": "^4.1.0",
    "sucrase": "^3.35.1",
    "ts-interface-checker": "^1.0.2"
  }
}
